# YouTube Data API v3 Key
# Get your key at: https://console.cloud.google.com/apis/credentials
#
# DO NOT put your real API key here! Instead:
# 1. Copy .envrc.local.example to .envrc.local
# 2. Add your real API key to .envrc.local
# 3. .envrc.local is gitignored and safe for secrets
export YOUTUBE_API_KEY="your-api-key-here"

# Optional: Langfuse tracing (for observability)
# export LANGFUSE_PUBLIC_KEY=""
# export LANGFUSE_SECRET_KEY=""
# export LANGFUSE_HOST="https://cloud.langfuse.com"

# Python unbuffered output (required for MCP stdio protocol)
export PYTHONUNBUFFERED=1

# Project-specific settings
export PYTHONPATH="$PWD/src:$PWD"

# Load local overrides (gitignored, for real secrets)
# Create this file with: cp .envrc.local.example .envrc.local
if [ -f .envrc.local ]; then
  source_env .envrc.local
fi
